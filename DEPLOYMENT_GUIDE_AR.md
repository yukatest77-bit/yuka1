# 🚀 دليل نشر التطبيق الجديد (Simplified Deployment Guide)

تم تحديث التطبيق ليعمل بالكامل (واجهة وخلفية) كوحدة واحدة. الآن، كل ما عليك فعله هو نشره على **Netlify** فقط، ولا حاجة لمنصة Render أو أي خادم آخر.

---

## 🟢 الخطوة الوحيدة: النشر على Netlify

إذا كنت قد أنشأت الموقع على Netlify سابقاً، فاتبع الخطوات التالية لتحديثه. إذا كان موقعاً جديداً، ابدأ من البداية.

1. تأكد أنك قمت برفع (Push) كل التعديلات الجديدة إلى GitHub.
2. اذهب إلى لوحة تحكم **Netlify**.
3. ادخل على إعدادات موقعك: **Site configuration** -> **Environment variables**.
4. أضف المتغيرات التالية (ضرورية جداً لعمل "المطبخ" الخلفي):

| المفتاح (Key) | القيمة (Value) |
|:---|:---|
| `NEXT_PUBLIC_API_URL` | (رابط موقعك نفسه على Netlify) *مثلاً: `https://your-site.netlify.app`* |
| `FIREBASE_PROJECT_ID` | (من ملف .env) |
| `FIREBASE_CLIENT_EMAIL` | (من ملف .env) |
| `FIREBASE_PRIVATE_KEY` | (من ملف .env - انسخه كاملاً) |
| `FIREBASE_DATABASE_URL` | (من ملف .env) |
| `SCRAPER_URL` | `https://pharmaciedegardetanger.site/` |

1. بعد إضافة المتغيرات، اذهب إلى تبويب **Deploys** واضغط **Trigger deploy**.

---

## 🤖 تفعيل التحديث التلقائي (السكربت اليومي)

بما أننا استغنينا عن الخادم الدائم، نحتاج لطريقة لتشغيل "المكشطة" (Scraper) يومياً لجلب البيانات الجديدة.
سنستخدم خدمة مجانية بسيطة تسمى **Cron-Job.org** (أو أي خدمة مشابهة).

1. سجل حساباً مجانياً في [cron-job.org](https://cron-job.org/en/).
2. أنشئ وظيفة جديدة (**Create Cronjob**).
3. الرابط (URL): `https://رابط-موقعك-على-netlify/api/cron/scrape`
4. الجدول (Schedule): اختر **"Every day at 00:00"** (أو أي وقت تفضله).
5. احفظ الوظيفة.

الآن، سيقوم هذا الموقع بزيارة رابط التحديث في موقعك يومياً، مما يشغل السكربت ويحدث البيانات في Firebase.

---

## ✅ التحقق

افتح موقعك على Netlify. يجب أن يظهر لك قائمة الصيدليات وتعمل كل الميزات (الأقرب إليك، البحث) بدون أي مشاكل، لأن الكل أصبح في مكان واحد!
